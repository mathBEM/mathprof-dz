# تشغيل محلي

1. ثبّت Node.js حديثًا.
2. من مجلد `server` نفّذ:
   `npm install`
3. ثم:
   `npm start`
4. افتح:
   `http://localhost:3000`
5. فحص الخدمة:
   `http://localhost:3000/api/health`

هذه النسخة لا تحتوي بعد على بيانات اعتماد أو قاعدة بيانات حقيقية. لا تضع أي مفاتيح سرية داخل الملفات.
